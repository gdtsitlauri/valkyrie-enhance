# Quality Benchmarks

The files in this directory are committed as local research artifacts for VALKYRIE's quality-evaluation layer.

## Current Interpretation

- `media_benchmark_summary.md` is a repository-internal diagnostic summary.
- The current committed snapshot uses a proxy reference workflow.
- These files are suitable for regression tracking and implementation comparison.
- They are not sufficient on their own for a broad public claim of benchmark superiority.

## To Produce Stronger Public Results

- regenerate the benchmark with a strict LR->HR reference protocol
- document the downsampling pipeline explicitly
- separate internal diagnostics from paper-facing leaderboard tables

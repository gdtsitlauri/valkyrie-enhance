# Ablation Results

Place per-module contribution studies and seed summaries here.

Current `module_ablation.json` values are reported as delta-PSNR against the full reference pipeline on the synthetic benchmark suite. Negative values indicate the disabled module improved the proxy metric for that synthetic setup, which is useful for tuning rather than an error.
